#include "main.h"
#include <stdio.h>

/**
 * main - Entry point
 * @argc: argument count
 * @argv: argument vector
 * Return:  Always 0 on successful execution
 */

int main(int argc, char *argv[])
{
	(void)argc;

	printf("%s/n", *argv);

	return (0);
}
